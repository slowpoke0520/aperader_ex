# ApeRadar 发布更新

软件会读取本仓库的 GitHub Releases，并寻找固定名称的文件 `ApeRadar-win-x64.zip`。稳定渠道忽略 prerelease；开发渠道同时比较正式版和 prerelease，选择版本号最高且可安装的版本。

## 发布新版本

稳定版使用 `原版本-ex.增强版序号`，明确区别于原项目的官方版本：

```powershell
.\Publish-Release.ps1 -Version 2.1.1-ex.10
git add .
git commit -m "Release 2.1.1-ex.10"
git tag v2.1.1-ex.10
git push origin codex/personal-improvement-center
git push origin v2.1.1-ex.10
```

推送标签后，GitHub Actions 会自动编译并创建 Release。完成后，旧版本点击“检查更新”即可下载安装。

开发版在稳定版本号后增加阶段和递增序号，例如：

```powershell
.\Publish-Release.ps1 -Version 2.1.1-ex.12-dev.10
git add .
git commit -m "Release 2.1.1-ex.12-dev.10"
git tag v2.1.1-ex.12-dev.10
git push origin codex/modern-roster-ui
git push origin v2.1.1-ex.12-dev.10
```

允许的阶段为 `dev`、`alpha`、`beta`、`rc`。GitHub Actions 会自动将这些版本创建为 prerelease；不要手工把开发版本改成正式 Release。

## 注意事项

- `2.1.1-ex.12-dev.5` 读取 GitHub Release 发布时间时会发生类型转换错误并误报“版本清单无效”，不能靠自身发现后续版本；需手动覆盖或通过随包独立更新器升级到 `dev.7` 一次。

- 稳定标签采用 `v原版本-ex.增强版序号`，例如 `v2.1.1-ex.11`；开发标签例如 `v2.1.1-ex.12-dev.1`。
- 稳定版不能是草稿或预发布版本；开发版必须由工作流标记为 prerelease。草稿版本不会进入任何软件更新渠道。
- 不要修改更新包文件名。
- 更新会保留名单、缓存、遭遇历史、窗口位置、日志和截图。
- 对局历史固定存放在 `%LocalAppData%\ApeRadar EX\History`，更新器不会接触该目录。
- 发布脚本会运行测试、清理旧发布输出，并生成 .NET 8 Windows x64 自包含主程序和独立更新器。
- 发布脚本会强制联网更新内置数据：从 WoWS Info 最新正式服非 PT 标签生成船名列表，并固定 PR `API` 分支的最新数据提交；无法下载、格式异常、条目过少或版本回退时发布直接失败。
- `Resources/Json/release_data.json` 记录两类数据的版本、日期、来源提交和 SHA-256。运行发布脚本后，需要将它与 `ships.json`、`expected_values.json` 一并加入发布提交。
- 更新器只保留版本确实更新的本机船名和 PR 数据；本机文件较旧或相同则由发布包中的数据替换。
- 发布前必须在未安装 .NET 的 Windows x64 环境验证干净启动，并从上一版本执行一次软件内升级。
- 涉及 Replay 的版本必须使用当前游戏版本的完整随机战录像完成端到端验证；API 兜底成功不能代替 Replay 验收。
