# ApeRadar 发布更新

软件内的“检查更新”会读取此仓库最新的 GitHub Release，并寻找固定名称的文件 `ApeRadar-win-x64.zip`。

## 发布新版本

增强版使用 `原版本-ex.增强版序号`，明确区别于原项目的官方版本：

```powershell
.\Publish-Release.ps1 -Version 2.1.1-ex.6
git add .
git commit -m "Release 2.1.1-ex.6"
git tag v2.1.1-ex.6
git push origin codex/history-center
git push origin v2.1.1-ex.6
```

推送标签后，GitHub Actions 会自动编译并创建 Release。完成后，旧版本点击“检查更新”即可下载安装。

## 注意事项

- 标签采用 `v原版本-ex.增强版序号`，例如 `v2.1.1-ex.1`。
- Release 不能是草稿或预发布版本。
- 不要修改更新包文件名。
- 更新会保留名单、缓存、遭遇历史、窗口位置、日志和截图。
- 对局历史固定存放在 `%LocalAppData%\ApeRadar EX\History`，更新器不会接触该目录。
- 发布脚本会运行测试、清理旧发布输出，并生成 .NET 8 Windows x64 自包含主程序和独立更新器。
- 发布前必须在未安装 .NET 的 Windows x64 环境验证干净启动，并从上一版本执行一次软件内升级。
- 涉及 Replay 的版本必须使用当前游戏版本的完整随机战录像完成端到端验证；API 兜底成功不能代替 Replay 验收。
