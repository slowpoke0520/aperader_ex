# ApeRadar 发布更新

软件内的“检查更新”会读取此仓库最新的 GitHub Release，并寻找固定名称的文件 `ApeRadar-win-x64.zip`。

## 发布新版本

首个增强版使用 `2.1.1-ex.1`，明确区别于原项目的官方版本：

```powershell
.\Publish-Release.ps1 -Version 2.1.1-ex.1
git add .
git commit -m "Release 2.1.1-ex.1"
git tag v2.1.1-ex.1
git push origin master
git push origin v2.1.1-ex.1
```

推送标签后，GitHub Actions 会自动编译并创建 Release。完成后，旧版本点击“检查更新”即可下载安装。

## 注意事项

- 标签采用 `v原版本-ex.增强版序号`，例如 `v2.1.1-ex.1`。
- Release 不能是草稿或预发布版本。
- 不要修改更新包文件名。
- 更新会保留名单、缓存、遭遇历史、窗口位置、日志和截图。
- 发布前应在本机运行脚本并确认编译成功。
