# 更新记录

本文档记录 ApeRadar EX 面向用户的重要变化。版本格式为 `原项目版本-ex.增强版序号`。

## [2.1.1-ex.12-dev.10] - 2026-09-15

### 现代化阵容界面

- 主界面改为阵容优先的五组语义布局：玩家、账号、单船、同级和状态。账号、单船与同级数据均使用带短标签的三行结构，不再依赖上下行位置猜测统计口径。
- 双方列表默认使用完整窗口宽度；“图表与分析”改为右侧覆盖抽屉，默认关闭，打开和关闭都不会压缩或重新排列阵容。
- 行高会按可用高度和双方最大人数在 58～68 DIP 内统一计算；1920×1080、100% 缩放、最大化时可完整显示标准 12v12，较小窗口或高 DPI 环境自动启用滚动且不重叠文字。
- 底部改为单行会话摘要、最新通知和自适应命令栏；通知历史按需弹出，窄屏时软件更新、设置和截图收入“更多”菜单。

### 显示与可读性

- 使用统一浅色资源规范整理字体、间距、圆角、边框、悬浮和选中状态；数据行采用轻微交替底色，胜率、PR 和伤害只为数值着色并保留文字标签。
- 玩家名、军团名和舰船名在空间不足时省略，悬浮仍可查看完整内容；数字使用统一字体和粗细，长场次、经验与六位伤害不会覆盖相邻列。
- WG 白色舰种图标继续使用深灰蓝色高对比徽标。全新安装默认显示完整统计、同级数据和舰种图标；升级用户保留原有设置，可随时在设置中调整。

### 结构、测试与发布

- 新增 `IRosterPresentationService`、`PlayerRosterRowViewModel`、`MetricGroupViewModel` 和纯计算 `RosterLayoutMetrics`，设置变化只重新生成展示模型，不会重新请求玩家数据。
- 排序继续使用原始玩家数值；双方共享相同列宽、行高和虚拟化表格。自动化测试覆盖展示分组、隐藏项目重排、原始值排序、分析抽屉不挤压阵容及 800×500～1920×1040 的中英文实际渲染。
- 发布包同步并校验最新版正式服船名列表、PR 期望数据及来源 SHA-256。

## [2.1.1-ex.12-dev.9] - 2026-09-15

### 舰种图标可读性

- 修复官方舰种图标以浅色透明图直接显示在白色背景上，导致几乎无法辨认的问题。
- 五种官方图标统一显示为 22×22 DIP 的深海军蓝高对比徽标，并增加浅色描边；原始图片保持不变，舰种名称仍可通过鼠标悬浮查看。
- 主阵容、玩家详情、历史筛选、历史列表、单场详情和图表悬浮提示共用同一个徽标控件，避免不同页面再次出现颜色或尺寸不一致。
- 增加徽标背景亮度、显示开关即时刷新和图标资源加载回归测试。
- 发布包继续同步并校验最新版正式服船名列表、PR 期望数据及来源 SHA-256。

## [2.1.1-ex.12-dev.8] - 2026-09-15

### 阵容数据显示修复

- 修复 1920×1080、100% 缩放且右侧分析区展开时，账号、单船、同阶和 PR 的长数值越过单元格并互相遮挡的问题。
- 完整统计列改用经过实际长数据验证的固定安全宽度；玩家名和舰船名空间不足时使用省略号，并始终为可选舰种图标保留独立位置。
- 紧凑模式不再根据窗口总宽度判断，而是根据每支队伍表格真正获得的宽度判断；右侧分析区展开或收起后会立即重新选择合适列布局。
- UI 回归测试增加五位场次、六位伤害、经验、同阶 PR 和长玩家名，并逐项验证可见单元格内容没有越过边界。
- 发布包继续同步并校验最新版正式服船名列表、PR 期望数据及来源 SHA-256。

## [2.1.1-ex.12-dev.7] - 2026-09-14

### 软件更新修复

- 修复 GitHub Release 的 `published_at` 被 Newtonsoft 解析为 `DateTime` 后，更新检查强制转换为 `DateTimeOffset` 而报“版本清单无效”的问题；现在同时兼容日期对象、ISO 8601 字符串和缺失发布时间。
- 增加按 GitHub 实际 Release 响应构造的回归测试，确保开发渠道可以发现 `dev.7` 及后续预发布版本。
- `dev.5` 的故障发生在独立更新器启动之前，因此该版本需要手动覆盖一次或由外部调用随包更新器；升级到 `dev.7` 后，后续版本可继续在软件内一键更新。
- 发布包继续同步并校验最新版正式服船名列表、PR 期望数据及来源 SHA-256。

## [2.1.1-ex.12-dev.6] - 2026-09-14

### 阵容界面

- 双方阵容的账号、单船和同阶统计统一压缩为严格两行，行高会根据可用高度和队伍人数在约 42～50 DIP 内自动调整；1920×1080、100% 缩放、最大化且通知折叠时可完整显示标准 12v12 阵容。
- 通知区默认只保留最新一条单行摘要，可通过左侧按钮展开为最高 120 DIP 的通知抽屉；底部会话、操作按钮和版本信息不再预留固定高度。
- 继续使用原生 DPI 布局；极小窗口、超大字体或超过 12 人的特殊阵容仍可滚动，但双方保持相同行高且不会相互错位。

### 舰种图标与历史数据

- “设置 → 外观”增加默认关闭的“在舰船名称后显示舰种图标”开关，保存后立即刷新已显示的阵容和历史内容，无需重新查询战绩。
- 航母、战列舰、巡洋舰、驱逐舰和潜艇使用 18×18 DIP 的官方 Wiki 舰种图标，并覆盖主阵容、玩家详情、历史筛选、历史列表、单场详情和图表悬浮提示；未知舰种不显示图标或空白间距。
- 历史数据库升级到 schema v4，新对局保存舰种；旧记录优先从本机阵容快照回填，其余记录按当前船名列表中的 `ShipId` 回填。
- 新增 `THIRD_PARTY_NOTICES.md`，记录五张图标的官方来源、SHA-256、权属及非官方关联声明；图标不纳入项目 MIT 授权。

### 设置与修复

- “默认”按钮现在只恢复当前设置页面尚未保存的控件；“取消”会丢弃修改，“保存”后才持久化，其他页面保持不变。“特别关注名单”页面的默认按钮禁用且不会修改名单。
- WG Public API `application_id` 移至“设置 → 高级”的数据源区域，修复它与文本输出模板重叠的问题；更新、浏览、查看日志和清理缓存等操作按钮不会被恢复默认触发。

### 测试与发布

- 增加 12v12 高度计算、1920×1080 实际渲染、五种舰种图标、未知舰种、设置页独立恢复默认及数据库 v3→v4 迁移测试。
- 发布包内置正式服 15.8 船名列表（2026-09-10，1094 艘）与 2026-09-13 PR 期望数据（1040 条、971 艘可用），并校验来源提交和 SHA-256；程序继续采用 Windows x64、.NET 8 自包含发布。

## [2.1.1-ex.12-dev.5] - 2026-09-14

### 发布修复

- 修复 GitHub Actions 在虚拟屏幕宽度小于测试请求宽度时，将正确启用的紧凑布局误判为失败的问题；布局回归测试现在按 WPF 实际渲染宽度检查列数、分析侧栏和版本信息区域。
- GitHub Actions 升级到 Node.js 24 运行时对应的 `actions/checkout@v6` 与 `actions/setup-dotnet@v5`，消除旧 Node.js 20 Action 的弃用警告。
- 本版本完整包含 `dev.2`～`dev.4` 的自适应界面、渐进式阵容加载、分层缓存、网络限流、历史分页、Replay 队列治理、崩溃诊断和更新状态改进。
- 发布包继续强制刷新并校验正式服船名列表、PR 期望值及来源清单；本版本内置船名列表为 15.8（2026-09-10，1094 艘），PR 期望数据为 2026-09-12（971 艘可用）。

### 测试

- Release 构建通过 71 项自动化测试；发布工作流会在创建 Release 前再次执行同一组测试并生成 SHA-256 校验文件。

## [2.1.1-ex.12-dev.4] - 2026-09-14

### 可靠性与发布

- 增加统一异常处理：UI、应用域和未观察后台任务的异常会记录版本、运行阶段、系统、运行时和完整异常；无法恢复的 UI 错误会显示 `%LocalAppData%\ApeRadar EX\Diagnostics` 中的诊断文件位置后安全退出。
- README 详细补充自适应界面、紧凑列、渐进式阵容加载、缓存位置、网络状态、对局历史分页、更新检查结果和崩溃报告使用说明。
- GitHub Release 说明改为直接使用 `CHANGELOG.md` 中当前版本内容，并同时发布 `ApeRadar-win-x64.zip.sha256`，确保每个开发版和稳定版都有与软件内“查看更新日志”一致的更新记录。
- 发布包继续强制刷新并校验正式服船名列表、PR 期望值及来源清单；本版本内置船名列表为 15.8（2026-09-10，1094 艘），PR 期望数据为 2026-09-12（971 艘可用）。

### 测试

- Release 构建通过 71 项自动化测试，并生成 Windows x64、.NET 8 自包含安装包。

## [2.1.1-ex.12-dev.3] - 2026-09-14

### 对局历史与 Replay

- 对局记录改为由 SQLite 每页读取 100 条，翻页不会一次创建全部列表项；服务器、账号、舰船和日期筛选使用可命中索引的动态条件。
- 筛选输入采用 250 毫秒合并处理，新查询会取消尚未完成的旧查询，避免连续切换条件时旧结果覆盖新界面。
- 为账号时间、常用组合筛选及待补查时间增加索引；最近时段使用数据库 `LIMIT 1` 直接读取。
- Replay 监听完成后立即从候选队列移除文件，另以轻量集合避免重复处理；兜底扫描频率由 2 分钟调整为 15 分钟，长期运行时队列不再持续增长。
- 取消导入不再计入失败数量，重新启动或恢复后可以继续处理。

### 测试

- 新增 205 条历史记录分页及倒序边界测试，验证第二页严格返回第 101～200 条且不会重复首尾记录。

## [2.1.1-ex.12-dev.2] - 2026-09-13

### 界面与显示

- 主雷达与设置窗口移除全局 `Viewbox`，统一使用“微软雅黑 UI / Segoe UI”字体回退、浅色卡片、按钮、表格、状态和错误色彩；中文图表及提示不再依赖缺少中文字形的默认字体。
- 主雷达改为阵容优先布局：常规宽度完整显示双方列表，窄屏自动切换为“玩家/舰船、单船、同阶、PR”紧凑列；分析图表在 1440 以下默认折叠，可按“图表与分析”打开，极窄屏会在阵容和分析之间切换。
- 当前会话、通知、常用操作和版本信息改为互不遮挡的独立区域；设置窗口内容可滚动，保存、取消和默认按钮固定在底部。
- 修复窗口由宽屏缩到紧凑模式后统计列被压成 20 像素、只显示半个标题的问题，并修复只读同阶标记在紧凑模板中被误作双向绑定而可能导致闪退的问题。

### 数据加载与网络

- 新增阵容加载协调器：先显示本地阵容，再使用缓存，最后后台补齐网络数据；切换到新对局时取消上一场请求，并以加载代次阻止旧结果覆盖新界面。
- API 结果保留网络失败、超时、限流、无效响应等原因；跨服查询一侧失败时仍保留另一侧和本地阵容，不再用空白表格表示错误。
- 网络请求增加全局 10 个、单来源 6 个并发限制，支持 `Retry-After`、短暂错误退避重试、相同 GET 合并及取消；确定性格式错误不会反复请求。
- 玩家缓存迁移到 `%LocalAppData%\ApeRadar EX\Cache\PlayerDataCache.v2.json`，使用原子保存和 30 天未使用清理；当前舰船、账号/同阶、身份数据分别采用 1 小时、6 小时和 7 天有效期口径。
- 本地日志增加阵容加载耗时、实际 HTTP 请求、重试次数及缓存命中率，不上传遥测。

### 更新与测试

- 高级设置会显示当前软件版本、最后检查时间及明确检查结果；区分已经最新、网络失败、限流、清单无效、资源缺失和 SHA-256 缺失，更新说明可通过悬浮查看。
- UI 回归测试覆盖中英文、800×500 紧凑窗口、1280×720、1600×900 和宽窄模式往返切换；新增 API 错误分类、取消、缓存有效期和空阵容数值测试。

## [2.1.1-ex.12-dev.1] - 2026-09-13

### 新功能

- “设置 → 高级”增加“在玩家列表显示同阶战绩”开关，默认关闭；开启后额外显示玩家在当前舰船等级的总场次、胜率和 PR。
- 同阶样本不足 50 场时显示警告，悬浮详情提供邻近等级胜率和 PR 作参考；11 级使用 10–11 级参考，其他等级使用相邻一级范围，但参考值不会替代真实同阶数据。
- 悬浮详情显示玩家对局最多的等级及占比；当高等级玩家的已识别数据中至少 60% 集中在 1–5 级时，提示“总胜率可能高估高级表现”。

### 数据与界面

- 跨级混房按每位玩家当前驾驶舰船的等级分别计算，不用房间最高等级套用全队。
- 同阶胜率和 PR 沿用现有色阶；镜像玩家列表的标题、数据和说明顺序保持一致。
- 同阶统计直接复用账号 PR 查询已返回的全舰船数据，不增加 API 请求数。

## [2.1.1-ex.11] - 2026-09-12

### 修复

- 修复游戏中途掉线并重连后，两段 Replay 被错误记录为两场对局的问题；程序会结合账号、舰船、地图、完整阵容和单局时限识别重连片段，避免把正常的下一局误合并。
- 玩家阵亡后提前退出时，保留 Replay 截止退出时已经确定的伤害、击沉和阵亡时间，不再把这些数据全部丢弃。
- 阵亡退出或掉线产生的不完整 Replay 不会立即执行战绩补差；程序会等到该局最晚可能结束时间之后再查询，避免服务器对局尚未结算时浪费补查机会。
- Replay 解析库升级至 `3.0.45-beta`，恢复对当前游戏 15.8 录像的解析支持。

## [2.1.1-ex.10] - 2026-09-11

### 新功能

- 对局历史升级为个人提升中心，增加“本次时段、舰船趋势、对局记录、时段历史”四个页面。
- “设置 → 高级”增加软件更新渠道，可选择“稳定版”或“开发版”；稳定版只接收正式 Release，开发版同时接收带有预发布标记的测试版本和后续正式版本。
- 手动点击“软件更新”或“检查并更新”后会按所选渠道直接启动带进度窗口的独立更新器，不再额外弹出一次确认；启动时自动检查只提醒，不会自动安装。
- 按服务器和本机账号自动划分连续游戏时段；相邻随机战间隔不超过 45 分钟时归入同一时段，并支持人工合并与分割。
- 主窗口增加低干扰的本次时段摘要，显示场次、胜率、场均伤害和平均 PR，点击可直接进入详细分析。
- 小于 5 场时不再把单场结果显示成夸张的百分比胜率，改为直接列出胜、未胜和待确认场数；曲线少于 3 个有效点时不绘制趋势，3～9 个点时明确提示波动很大。
- Replay 增加生存结果、生存时间、战斗时长和潜在伤害解析；承受伤害及原始伤害类型作为可选实验指标。
- 单局详情增加双方战前阵容快照、数据可用性、收藏、复盘标签和自由备注。
- 为同一舰船提供本次时段与个人历史基线对比；只有本次至少 5 场、历史至少 20 场时才生成判断，避免小样本误导。

### 数据与可靠性

- 历史数据库升级至 Schema v2，迁移前自动创建 `history.db.pre-v2-时间.bak`，并为旧记录自动补建时段。
- 高级指标逐项记录稳定性和解析规则版本；缺失值保持为空，不会按 0 参与平均值或曲线。
- Replay 解析规则升级后可重新处理已有录像哈希并更新原对局，不重复创建记录或删除已有阵容。
- 新增实验指标总开关，默认关闭；实验数据在界面中明确标识。
- 修复点击“对局历史”时，PR 数据版本只读属性被 `Run.Text` 误用双向绑定而触发的 WPF 未处理异常和程序闪退。
- 重排历史窗口：汇总卡片改为多行，详情和时段列表改为上下结构，状态信息与底部操作按钮分行显示，修复窄窗口及高 DPI 下的裁切和重叠。
- 修复主窗口消息列表跨行占位，与底部“本次时段”摘要横条叠在一起并遮挡消息内容的问题；消息、按钮、时段摘要和版本信息现在使用互不覆盖的独立区域。
- 发布流程现在强制刷新内置数据：船名列表从 WoWS Info 最新正式服、非 PT 的不可变标签生成，PR 期望值固定到上游 `API` 分支的最新文件提交；下载、结构、数量或防降级校验失败时立即终止发布。
- 内置船名列表更新至正式服 15.8（2026-09-10，1094 艘），PR 数据更新至 2026-09-10（1029 条、966 条可用期望值），并随包附带来源提交及 SHA-256 清单。
- 软件更新器现在比较船名列表日期和 PR 数据时间戳：本机数据确实更新时继续保留，本机数据较旧或同版本时使用发布包数据，不再让旧文件遮住新版内置数据。
- 发布流程支持 `dev`、`alpha`、`beta`、`rc` 开发版本号；GitHub Actions 自动把这些标签发布为 prerelease，正式版本仍进入稳定渠道。

### 测试

- 增加数据库 v1→v2 迁移、45 分钟时段边界、人工合并/分割、复盘持久化、高级指标、实验开关和样本门槛测试。
- 增加历史窗口实际显示与布局冒烟测试，覆盖仅构造窗口时无法发现的运行时绑定错误。
- 布局测试覆盖中英文及 860×620、1000×700、1180×760 三种窗口尺寸，并逐页检查筛选区、摘要卡片和底部按钮没有重叠。
- 增加更新器数据版本选择测试，覆盖“保留本机较新数据”和“发布包替换本机旧数据”。
- 增加更新渠道筛选和开发版本排序测试，确保稳定用户不会收到预发布版本，开发用户可在同一修订号的正式版发布后自动转入正式版。
- 使用本机当前游戏版本的 30 个 Replay 完成端到端验证：25 场随机战均成功解析或按提前退出正确降级，5 场非随机战正确跳过；真实录像未加入仓库。

## [2.1.1-ex.9] - 2026-09-07

### 修复

- 修复对局历史折线图的横轴舰船中文名和悬浮提示使用 LiveCharts 默认字体，因缺少中文字形而显示方框的问题；坐标轴与提示现在统一使用系统中实际支持中文的字体。
- 修复主程序退出时后台历史任务与界面线程互相等待，导致更新器等待 60 秒后仍无法覆盖 `Accessibility.dll` 等正在使用的程序集的问题。
- 更新器现在会等待主程序正常退出；超时后只会结束本次更新所对应的 ApeRadar 进程，不会误结束其他运行实例。
- 软件更新使用独立临时下载目录并阻止重复更新任务，避免启动检查和手动检查同时运行时互相删除或覆盖文件。
- 移除缺少独立更新器时将主程序复制到临时目录运行的无效回退逻辑，改为给出明确错误。

### 改进

- 增加独立的更新进度窗口，从下载开始显示百分比、已下载容量，以及等待退出、校验、解压、备份、安装、验证和启动等阶段；更新过程可安全取消。
- 安装前完整备份将要覆盖的文件；安装或新版本启动失败时自动回滚，并尝试重新启动原版本。
- 更新失败日志移动到 `%LocalAppData%\ApeRadar EX\Update\UpdateError.log`，避免安装目录无写入权限时丢失诊断信息。
- 软件更新保留用户已单独下载的船名列表和 PR 数据，缺少这些文件时仍会安装包内默认版本。

### 测试

- 增加更新文件事务、失败回滚、用户数据保留和恶意压缩包路径拦截测试。

## [2.1.1-ex.8] - 2026-09-06

### 改进

- 对局历史的单场及筛选汇总“伤害”“击沉”增加分级颜色：按每艘舰船当前 PR 期望值计算相对表现，再映射到与 PR 一致的 8 档高对比度文字和淡色标签，避免不同舰种、等级使用固定绝对阈值造成误判。
- 对局历史地图列不再直接显示 Replay 内部代号；内置亚服官方中英文地图名称映射，并兼容旧数据库中的 `spaces/...`、`IDS_MAP_...` 和英文名称。

### 测试

- 增加伤害/击沉标准化评分边界和地图代号转换测试，未知的新地图代号仍会原样显示，避免空白。

## [2.1.1-ex.7] - 2026-09-06

### 改进

- 将“软件启动时自动检查更新”移动到“设置 → 高级”，并补充其作用说明；它只控制软件版本检查，关闭后仍可手动检查软件、船名列表和 PR 数据更新。
- 高级设置增加“查看更新日志”，可直接打开 GitHub Releases 中各版本的更新说明。
- 对局历史的胜负与 PR 改为高对比度文字搭配淡色标签：胜利为绿色、失败为红色，PR 按分级显示对应颜色，弱化大面积高饱和色以提升长时间查看时的易读性。
- 发布脚本仅清理当前发布的暂存目录和压缩包，运行中的旧测试副本不再阻塞新版本打包。

## [2.1.1-ex.6] - 2026-09-05

### 新功能

- 增加“对局历史”中心，仅自动记录本机玩家的随机战；支持服务器、账号、舰船和日期筛选。
- 增加单船长期追踪，可查看场次、胜率、场均伤害、场均击沉、PR 和数据完整率，以及 10、20、50 场或全部场次的滚动曲线。
- 自动监听并首次导入游戏目录中的 Replay，等待文件写入完成、按文件哈希去重，并提供暂停、重新扫描和失败重试。
- Replay 数据不完整或版本不支持时，通过战前、战后本机单船累计数据差值低频补查；多场差值保存为合并区间，不伪造单场记录。
- 高级设置增加可选的 WG Public API `application_id`，Vortex 仍为默认数据源。

### 数据与隐私

- 新增本地 SQLite 历史库，固定存放于 `%LocalAppData%\ApeRadar EX\History\history.db`，使用 WAL、事务、外键和唯一对局/Replay 标识。
- 保存每场战前双方阵容快照，为后续对局平衡分析保留真实样本；本版本不展示因果结论。
- 历史记录默认永久保留、不上传，并且不受软件更新、重新解压或安装目录变化影响。
- 数据库确认损坏时备份原库及 WAL 文件，再自动建立新库。

### 界面与可靠性

- 主窗口增加独立的“对局历史”按钮，并使用可换行布局避免高 DPI 或窄窗口下按钮重叠。
- 修复点击“对局历史”时，PR 数据版本的只读属性被 WPF 误用双向绑定而导致程序闪退的问题。
- 修复游戏将随机战 Replay 标记为 `matchGroup: pvp` 时被错误统计为“跳过”的问题，并同步修正新对局的随机战识别。
- 将 Replay 解析库升级至 `3.0.40-beta`，加入游戏 15.7 Replay 定义，修复 15.7 录像全部提示数据流读取失败的问题。
- 完整 Replay 现在从服务器伤害统计、舰船击沉事件和最终获胜队伍中提取本机单场数据；提前结束的录像保留为待补查，不再伪造完整结果。
- 历史窗口明确显示 Replay 精确、API 精确、API 合并和仅元数据等来源，以及完整、待补查、不支持和失败状态。
- 网络失败不再耗尽补查次数；任务会保持待处理并支持程序重启后继续。
- PR 按当前期望值重新计算，并在历史窗口显示 PR 数据版本。
- 对局历史的胜利、失败、平局/未获胜和待确认结果分别使用绿色、红色、黄色和灰色；单场 PR 与平均 PR 沿用 PR 分级颜色。

### 运行环境与发布

- 项目升级至 .NET 8，Release 改为 Windows x64 自包含包，用户无需另行安装 .NET。
- 引入固定版本的 SQLite 与 Replay 解析适配层，业务代码不直接依赖解析库类型。
- 新增独立、单文件、自包含更新器，支持从旧运行时版本安全升级。
- GitHub Actions 和发布脚本升级至 .NET 8；发布前自动运行测试并清理旧发布目录。
- 新增 xUnit 测试，覆盖数据库写入去重、API 单场/多场差值、待补查恢复、滚动窗口、加权汇总和元数据完整率。

> 发布门槛：必须使用当前游戏版本的完整随机战 Replay 完成本地端到端验收后，才能创建并推送 `v2.1.1-ex.6` 正式标签。

## [2.1.1-ex.5] - 2026-08-31

### 新功能

- 支持通过玩家右键菜单添加金色 `★` 自定义标记，标记随 `WatchList.json` 持久保存。
- 近期重遇玩家的悬浮提示现在会列出每次遭遇的时间、敌我阵营和当时使用的舰船；旧版历史记录保持兼容。

## [2.1.1-ex.4] - 2026-08-31

### 修复

- 将发布包内置的船名列表从 12.5（20230624）更新至 15.5（20260621），并强制在每次构建时复制该资源，避免旧构建产物进入安装包以及新版本首次启动仍要求重复更新。
- 修复设置窗口“外观”页面最右侧 PR 显示选项被裁切的问题。
- 修复设置窗口“通用”页面软件更新按钮与船名列表更新按钮视觉重叠的问题。
- 修复船名列表和 PR 数据更新按钮因高度与边距超过行高而被垂直压扁的问题，并统一按钮尺寸。

### 开发流程

- 发布脚本现在强制要求 `CHANGELOG.md` 中存在对应版本章节，避免发布时遗漏更新记录。

## [2.1.1-ex.3] - 2026-08-31

### 修复

- 恢复独立的船名列表在线更新功能。
- 修复切换到 GitHub 软件更新后，`includeShipList` 参数失效造成的功能回退。

### 改进

- 将设置页面的数据更新入口明确拆分为“检查软件更新”“更新船名列表”和“更新 PR 数据”。
- 启动时依次检查软件版本和船名列表版本，两条更新链路互不覆盖。
- 船名列表下载增加 SHA-256 校验和 ZIP 路径安全检查。
- 船名列表更新完成后立即刷新界面上的版本信息。

## [2.1.1-ex.2] - 2026-08-31

### 修复

- 修复 GitHub Release 的 `published_at` 字段由 Newtonsoft.Json 解析为 `DateTime` 时发生的类型转换异常。
- 修复点击“检查更新”后提示查看日志、无法继续比较版本的问题。

> `2.1.1-ex.1` 的更新检查在读取发布时间时会失败，因此需要手动安装一次 `2.1.1-ex.2` 或更高版本。

## [2.1.1-ex.1] - 2026-08-31

首个以 EX 版本规则发布的增强版。

### 新增

- 使用 GitHub Releases 检查、下载和发布软件更新。
- 增加独立更新进程：等待主程序退出后替换文件并自动重新启动。
- 增加 GitHub Actions 自动发布流程和 `Publish-Release.ps1` 一键打包脚本。
- 增加最近对局玩家标记、固定队友和单玩家强制刷新功能。
- 增加落盘玩家数据缓存，重启程序后仍可快速显示数据。

### 修复

- 修正 PR 颜色档位：恢复 WoWS Numbers 标准 8 档阈值，移除导致颜色整体错位的额外灰色档。
- 修复详细信息中单船 PR 错误继承账号 PR 颜色的问题。
- 修复部分 Vortex API 数据字段缺失时的解析异常。
- 修复多个核心异步流程使用非事件 `async void` 和 `.Result` 带来的异常及阻塞风险。

### 安全与可靠性

- HTTP 请求现在会检查响应状态码。
- 软件包下载采用临时文件，成功后再替换目标文件。
- 软件更新强制使用 HTTPS，并校验 GitHub 提供的 SHA-256 摘要。
- ZIP 解压前检查路径穿越，阻止文件写出目标目录。
- 更新服务器不能再指定任意本地下载或删除目录。
- 关注名单、玩家缓存和遭遇历史改为原子写入，降低异常退出造成 JSON 损坏的风险。
- 修复玩家名称通过字符串拼接生成 JSON 时的转义问题。
- HTTP 请求计数改为线程安全。

### 性能

- 增加 HTTP 并发限制和 gzip/deflate 解压支持。
- 玩家数据采用 24 小时缓存，并减少每局重复 API 请求。

## 2.1.0 基线

- 基于 zylalx1 的 ApeRadar 2.1.0 源码建立 EX 分支。
- 保留玩家备注、关注名单、战绩显示和基础 PR 功能。

[2.1.1-ex.3]: https://github.com/slowpoke0520/aperader_ex/compare/v2.1.1-ex.2...v2.1.1-ex.3
[2.1.1-ex.4]: https://github.com/slowpoke0520/aperader_ex/compare/v2.1.1-ex.3...v2.1.1-ex.4
[2.1.1-ex.5]: https://github.com/slowpoke0520/aperader_ex/compare/v2.1.1-ex.4...v2.1.1-ex.5
[2.1.1-ex.6]: https://github.com/slowpoke0520/aperader_ex/compare/v2.1.1-ex.5...v2.1.1-ex.6
[2.1.1-ex.7]: https://github.com/slowpoke0520/aperader_ex/compare/v2.1.1-ex.6...v2.1.1-ex.7
[2.1.1-ex.8]: https://github.com/slowpoke0520/aperader_ex/compare/v2.1.1-ex.7...v2.1.1-ex.8
[2.1.1-ex.2]: https://github.com/slowpoke0520/aperader_ex/compare/v2.1.1-ex.1...v2.1.1-ex.2
[2.1.1-ex.1]: https://github.com/slowpoke0520/aperader_ex/releases/tag/v2.1.1-ex.1
