# 更新记录

本文档记录 ApeRadar EX 面向用户的重要变化。版本格式为 `原项目版本-ex.增强版序号`。

## 未发布

暂无。

## [2.1.1-ex.10] - 2026-09-09

### 新功能

- 对局历史升级为个人提升中心，增加“本次时段、舰船趋势、对局记录、时段历史”四个页面。
- 按服务器和本机账号自动划分连续游戏时段；相邻随机战间隔不超过 45 分钟时归入同一时段，并支持人工合并与分割。
- 主窗口增加低干扰的本次时段摘要，显示场次、胜率、场均伤害和平均 PR，点击可直接进入详细分析。
- Replay 增加生存结果、生存时间、战斗时长和潜在伤害解析；承受伤害及原始伤害类型作为可选实验指标。
- 单局详情增加双方战前阵容快照、数据可用性、收藏、复盘标签和自由备注。
- 为同一舰船提供本次时段与个人历史基线对比；只有本次至少 5 场、历史至少 20 场时才生成判断，避免小样本误导。

### 数据与可靠性

- 历史数据库升级至 Schema v2，迁移前自动创建 `history.db.pre-v2-时间.bak`，并为旧记录自动补建时段。
- 高级指标逐项记录稳定性和解析规则版本；缺失值保持为空，不会按 0 参与平均值或曲线。
- Replay 解析规则升级后可重新处理已有录像哈希并更新原对局，不重复创建记录或删除已有阵容。
- 新增实验指标总开关，默认关闭；实验数据在界面中明确标识。

### 测试

- 增加数据库 v1→v2 迁移、45 分钟时段边界、人工合并/分割、复盘持久化、高级指标、实验开关和样本门槛测试。
- 使用本机当前游戏版本的 30 个 Replay 完成端到端验证：25 场随机战均成功解析或按提前退出正确降级，5 场非随机战正确跳过；真实录像未加入仓库。

## [2.1.1-ex.9] - 2026-09-07

### 修复

- 修复对局历史折线图的横轴舰船中文名和悬浮提示使用 LiveCharts 默认字体，因缺少中文字形而显示方框的问题；坐标轴与提示现在统一使用系统中实际支持中文的字体。
- 修复主程序退出时后台历史任务与界面线程互相等待，导致更新器等待 60 秒后仍无法覆盖 `Accessibility.dll` 等正在使用的程序集的问题。
- 更新器现在会等待主程序正常退出；超时后只会结束本次更新所对应的 ApeRadar 进程，不会误结束其他运行实例。
- 软件更新使用独立临时下载目录并阻止重复更新任务，避免启动检查和手动检查同时运行时互相删除或覆盖文件。
- 移除缺少独立更新器时将主程序复制到临时目录运行的无效回退逻辑，改为给出明确错误。

### 改进

- 增加独立的更新进度窗口，从下载开始显示百分比、已下载容量，以及等待退出、校验、解压、备份、安装、验证和启动等阶段；更新过程可安全取消。
- 安装前完整备份将要覆盖的文件；安装或新版本启动失败时自动回滚，并尝试重新启动原版本。
- 更新失败日志移动到 `%LocalAppData%\ApeRadar EX\Update\UpdateError.log`，避免安装目录无写入权限时丢失诊断信息。
- 软件更新保留用户已单独下载的船名列表和 PR 数据，缺少这些文件时仍会安装包内默认版本。

### 测试

- 增加更新文件事务、失败回滚、用户数据保留和恶意压缩包路径拦截测试。

## [2.1.1-ex.8] - 2026-09-06

### 改进

- 对局历史的单场及筛选汇总“伤害”“击沉”增加分级颜色：按每艘舰船当前 PR 期望值计算相对表现，再映射到与 PR 一致的 8 档高对比度文字和淡色标签，避免不同舰种、等级使用固定绝对阈值造成误判。
- 对局历史地图列不再直接显示 Replay 内部代号；内置亚服官方中英文地图名称映射，并兼容旧数据库中的 `spaces/...`、`IDS_MAP_...` 和英文名称。

### 测试

- 增加伤害/击沉标准化评分边界和地图代号转换测试，未知的新地图代号仍会原样显示，避免空白。

## [2.1.1-ex.7] - 2026-09-06

### 改进

- 将“软件启动时自动检查更新”移动到“设置 → 高级”，并补充其作用说明；它只控制软件版本检查，关闭后仍可手动检查软件、船名列表和 PR 数据更新。
- 高级设置增加“查看更新日志”，可直接打开 GitHub Releases 中各版本的更新说明。
- 对局历史的胜负与 PR 改为高对比度文字搭配淡色标签：胜利为绿色、失败为红色，PR 按分级显示对应颜色，弱化大面积高饱和色以提升长时间查看时的易读性。
- 发布脚本仅清理当前发布的暂存目录和压缩包，运行中的旧测试副本不再阻塞新版本打包。

## [2.1.1-ex.6] - 2026-09-05

### 新功能

- 增加“对局历史”中心，仅自动记录本机玩家的随机战；支持服务器、账号、舰船和日期筛选。
- 增加单船长期追踪，可查看场次、胜率、场均伤害、场均击沉、PR 和数据完整率，以及 10、20、50 场或全部场次的滚动曲线。
- 自动监听并首次导入游戏目录中的 Replay，等待文件写入完成、按文件哈希去重，并提供暂停、重新扫描和失败重试。
- Replay 数据不完整或版本不支持时，通过战前、战后本机单船累计数据差值低频补查；多场差值保存为合并区间，不伪造单场记录。
- 高级设置增加可选的 WG Public API `application_id`，Vortex 仍为默认数据源。

### 数据与隐私

- 新增本地 SQLite 历史库，固定存放于 `%LocalAppData%\ApeRadar EX\History\history.db`，使用 WAL、事务、外键和唯一对局/Replay 标识。
- 保存每场战前双方阵容快照，为后续对局平衡分析保留真实样本；本版本不展示因果结论。
- 历史记录默认永久保留、不上传，并且不受软件更新、重新解压或安装目录变化影响。
- 数据库确认损坏时备份原库及 WAL 文件，再自动建立新库。

### 界面与可靠性

- 主窗口增加独立的“对局历史”按钮，并使用可换行布局避免高 DPI 或窄窗口下按钮重叠。
- 修复点击“对局历史”时，PR 数据版本的只读属性被 WPF 误用双向绑定而导致程序闪退的问题。
- 修复游戏将随机战 Replay 标记为 `matchGroup: pvp` 时被错误统计为“跳过”的问题，并同步修正新对局的随机战识别。
- 将 Replay 解析库升级至 `3.0.40-beta`，加入游戏 15.7 Replay 定义，修复 15.7 录像全部提示数据流读取失败的问题。
- 完整 Replay 现在从服务器伤害统计、舰船击沉事件和最终获胜队伍中提取本机单场数据；提前结束的录像保留为待补查，不再伪造完整结果。
- 历史窗口明确显示 Replay 精确、API 精确、API 合并和仅元数据等来源，以及完整、待补查、不支持和失败状态。
- 网络失败不再耗尽补查次数；任务会保持待处理并支持程序重启后继续。
- PR 按当前期望值重新计算，并在历史窗口显示 PR 数据版本。
- 对局历史的胜利、失败、平局/未获胜和待确认结果分别使用绿色、红色、黄色和灰色；单场 PR 与平均 PR 沿用 PR 分级颜色。

### 运行环境与发布

- 项目升级至 .NET 8，Release 改为 Windows x64 自包含包，用户无需另行安装 .NET。
- 引入固定版本的 SQLite 与 Replay 解析适配层，业务代码不直接依赖解析库类型。
- 新增独立、单文件、自包含更新器，支持从旧运行时版本安全升级。
- GitHub Actions 和发布脚本升级至 .NET 8；发布前自动运行测试并清理旧发布目录。
- 新增 xUnit 测试，覆盖数据库写入去重、API 单场/多场差值、待补查恢复、滚动窗口、加权汇总和元数据完整率。

> 发布门槛：必须使用当前游戏版本的完整随机战 Replay 完成本地端到端验收后，才能创建并推送 `v2.1.1-ex.6` 正式标签。

## [2.1.1-ex.5] - 2026-08-31

### 新功能

- 支持通过玩家右键菜单添加金色 `★` 自定义标记，标记随 `WatchList.json` 持久保存。
- 近期重遇玩家的悬浮提示现在会列出每次遭遇的时间、敌我阵营和当时使用的舰船；旧版历史记录保持兼容。

## [2.1.1-ex.4] - 2026-08-31

### 修复

- 将发布包内置的船名列表从 12.5（20230624）更新至 15.5（20260621），并强制在每次构建时复制该资源，避免旧构建产物进入安装包以及新版本首次启动仍要求重复更新。
- 修复设置窗口“外观”页面最右侧 PR 显示选项被裁切的问题。
- 修复设置窗口“通用”页面软件更新按钮与船名列表更新按钮视觉重叠的问题。
- 修复船名列表和 PR 数据更新按钮因高度与边距超过行高而被垂直压扁的问题，并统一按钮尺寸。

### 开发流程

- 发布脚本现在强制要求 `CHANGELOG.md` 中存在对应版本章节，避免发布时遗漏更新记录。

## [2.1.1-ex.3] - 2026-08-31

### 修复

- 恢复独立的船名列表在线更新功能。
- 修复切换到 GitHub 软件更新后，`includeShipList` 参数失效造成的功能回退。

### 改进

- 将设置页面的数据更新入口明确拆分为“检查软件更新”“更新船名列表”和“更新 PR 数据”。
- 启动时依次检查软件版本和船名列表版本，两条更新链路互不覆盖。
- 船名列表下载增加 SHA-256 校验和 ZIP 路径安全检查。
- 船名列表更新完成后立即刷新界面上的版本信息。

## [2.1.1-ex.2] - 2026-08-31

### 修复

- 修复 GitHub Release 的 `published_at` 字段由 Newtonsoft.Json 解析为 `DateTime` 时发生的类型转换异常。
- 修复点击“检查更新”后提示查看日志、无法继续比较版本的问题。

> `2.1.1-ex.1` 的更新检查在读取发布时间时会失败，因此需要手动安装一次 `2.1.1-ex.2` 或更高版本。

## [2.1.1-ex.1] - 2026-08-31

首个以 EX 版本规则发布的增强版。

### 新增

- 使用 GitHub Releases 检查、下载和发布软件更新。
- 增加独立更新进程：等待主程序退出后替换文件并自动重新启动。
- 增加 GitHub Actions 自动发布流程和 `Publish-Release.ps1` 一键打包脚本。
- 增加最近对局玩家标记、固定队友和单玩家强制刷新功能。
- 增加落盘玩家数据缓存，重启程序后仍可快速显示数据。

### 修复

- 修正 PR 颜色档位：恢复 WoWS Numbers 标准 8 档阈值，移除导致颜色整体错位的额外灰色档。
- 修复详细信息中单船 PR 错误继承账号 PR 颜色的问题。
- 修复部分 Vortex API 数据字段缺失时的解析异常。
- 修复多个核心异步流程使用非事件 `async void` 和 `.Result` 带来的异常及阻塞风险。

### 安全与可靠性

- HTTP 请求现在会检查响应状态码。
- 软件包下载采用临时文件，成功后再替换目标文件。
- 软件更新强制使用 HTTPS，并校验 GitHub 提供的 SHA-256 摘要。
- ZIP 解压前检查路径穿越，阻止文件写出目标目录。
- 更新服务器不能再指定任意本地下载或删除目录。
- 关注名单、玩家缓存和遭遇历史改为原子写入，降低异常退出造成 JSON 损坏的风险。
- 修复玩家名称通过字符串拼接生成 JSON 时的转义问题。
- HTTP 请求计数改为线程安全。

### 性能

- 增加 HTTP 并发限制和 gzip/deflate 解压支持。
- 玩家数据采用 24 小时缓存，并减少每局重复 API 请求。

## 2.1.0 基线

- 基于 zylalx1 的 ApeRadar 2.1.0 源码建立 EX 分支。
- 保留玩家备注、关注名单、战绩显示和基础 PR 功能。

[2.1.1-ex.3]: https://github.com/slowpoke0520/aperader_ex/compare/v2.1.1-ex.2...v2.1.1-ex.3
[2.1.1-ex.4]: https://github.com/slowpoke0520/aperader_ex/compare/v2.1.1-ex.3...v2.1.1-ex.4
[2.1.1-ex.5]: https://github.com/slowpoke0520/aperader_ex/compare/v2.1.1-ex.4...v2.1.1-ex.5
[2.1.1-ex.6]: https://github.com/slowpoke0520/aperader_ex/compare/v2.1.1-ex.5...v2.1.1-ex.6
[2.1.1-ex.7]: https://github.com/slowpoke0520/aperader_ex/compare/v2.1.1-ex.6...v2.1.1-ex.7
[2.1.1-ex.8]: https://github.com/slowpoke0520/aperader_ex/compare/v2.1.1-ex.7...v2.1.1-ex.8
[2.1.1-ex.2]: https://github.com/slowpoke0520/aperader_ex/compare/v2.1.1-ex.1...v2.1.1-ex.2
[2.1.1-ex.1]: https://github.com/slowpoke0520/aperader_ex/releases/tag/v2.1.1-ex.1
