<UserControl x:Class="ApeRadar.Controls.DashboardMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ApeRadar"
             xmlns:controls="clr-namespace:ApeRadar.Controls"
             xmlns:utils="clr-namespace:ApeRadar.Utils"
             xmlns:cvt="clr-namespace:ApeRadar.Utils.Converters"
             xmlns:p="clr-namespace:ApeRadar.Properties"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             x:Name="Root" UseLayoutRounding="True" SnapsToDevicePixels="True"
             PreviewKeyDown="Root_PreviewKeyDown" SizeChanged="Root_SizeChanged">
    <UserControl.Resources>
        <SolidColorBrush x:Key="DashboardBackground" Color="#F5F8FC"/>
        <SolidColorBrush x:Key="DashboardSurface" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="DashboardBorder" Color="#DCE5F0"/>
        <SolidColorBrush x:Key="DashboardText" Color="#13233B"/>
        <SolidColorBrush x:Key="DashboardMuted" Color="#718198"/>
        <SolidColorBrush x:Key="DashboardBlue" Color="#176ADE"/>
        <SolidColorBrush x:Key="DashboardRed" Color="#D92E49"/>
        <SolidColorBrush x:Key="DashboardAllySurface" Color="#EAF3FF"/>
        <SolidColorBrush x:Key="DashboardEnemySurface" Color="#FFF0F2"/>

        <cvt:RosterMetricBrushConverter x:Key="MetricBrush"/>
        <cvt:RosterBadgeBrushConverter x:Key="BadgeBrush"/>
        <cvt:ShipTierConverter x:Key="ShipTier"/>
        <cvt:LabelGamePathIsSetContentConverter x:Key="CvtGamePathIsSetText"/>
        <cvt:LabelGamePathIsSetColorConverter x:Key="CvtGamePathIsSetColor"/>
        <utils:ShipRelationBrushConverter x:Key="ShipRelationBrush"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <cvt:DataGridColumnVisibilityConverter x:Key="ColumnVisibility"/>
        <cvt:NotificationMessageColorConverter x:Key="NotificationColor"/>

        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource DashboardSurface}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DashboardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
        </Style>
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Height" Value="44"/>
            <Setter Property="Margin" Value="8,2"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Foreground" Value="#41536D"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style x:Key="NavButtonSelected" TargetType="Button" BasedOn="{StaticResource NavButton}">
            <Setter Property="Background" Value="#E6F0FF"/>
            <Setter Property="Foreground" Value="{StaticResource DashboardBlue}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D4DFEC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style x:Key="FilterButton" TargetType="Button">
            <Setter Property="Height" Value="24"/>
            <Setter Property="MinWidth" Value="48"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Background" Value="#F7F9FC"/>
            <Setter Property="BorderBrush" Value="#D8E1EC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style x:Key="FilterButtonActive" TargetType="Button" BasedOn="{StaticResource FilterButton}">
            <Setter Property="Background" Value="#E3EEFF"/>
            <Setter Property="BorderBrush" Value="#7FB0F5"/>
            <Setter Property="Foreground" Value="#155CB5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="SegmentButton" TargetType="Button" BasedOn="{StaticResource FilterButton}">
            <Setter Property="Margin" Value="0"/>
            <Setter Property="MinWidth" Value="48"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style x:Key="SegmentActive" TargetType="Button" BasedOn="{StaticResource SegmentButton}">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="{StaticResource DashboardBlue}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="RosterGrid" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserResizeColumns" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="CanUserSortColumns" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="All"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#DCE4EE"/>
            <Setter Property="VerticalGridLinesBrush" Value="#E2E9F2"/>
            <Setter Property="RowHeight" Value="43"/>
            <Setter Property="ColumnHeaderHeight" Value="30"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFD"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="EnableRowVirtualization" Value="True"/>
            <Setter Property="EnableColumnVirtualization" Value="True"/>
            <EventSetter Event="ScrollViewer.ScrollChanged" Handler="RosterGrid_ScrollChanged"/>
        </Style>
        <Style x:Key="RosterRowStyle" TargetType="DataGridRow">
            <EventSetter Event="MouseEnter" Handler="RosterRow_MouseEnter"/>
            <EventSetter Event="MouseLeave" Handler="RosterRow_MouseLeave"/>
        </Style>
        <Style x:Key="CellText" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei UI"/>
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="Foreground" Value="{StaticResource DashboardText}"/>
        </Style>

        <DataTemplate x:Key="BadgeTemplate">
            <Border CornerRadius="4" Padding="4,1" Margin="1,0"
                    Background="{Binding Severity, Converter={StaticResource BadgeBrush}, ConverterParameter=Background}"
                    ToolTip="{Binding ToolTip}">
                <TextBlock Text="{Binding DisplayText}" FontSize="8.5" FontWeight="SemiBold"
                           Foreground="{Binding Severity, Converter={StaticResource BadgeBrush}, ConverterParameter=Foreground}"/>
            </Border>
        </DataTemplate>
        <DataTemplate x:Key="PlayerTemplate">
            <Grid Margin="6,1" ClipToBounds="True">
                <Grid.RowDefinitions><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
                <Grid Grid.Row="0" ClipToBounds="True">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBlock Style="{StaticResource CellText}" FontSize="13" FontWeight="SemiBold" Text="{Binding PlayerDisplayName}" TextTrimming="CharacterEllipsis" ToolTip="{Binding PlayerDisplayName}"/>
                    <ItemsControl Grid.Column="1" ItemsSource="{Binding VisibleStatusBadges}" ItemTemplate="{StaticResource BadgeTemplate}" VerticalAlignment="Center">
                        <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                    </ItemsControl>
                    <TextBlock Grid.Column="2" Text="{Binding OverflowBadgeText}" Foreground="{StaticResource DashboardMuted}" FontSize="9" Margin="3,0,0,0" VerticalAlignment="Center" ToolTip="{Binding AllStatusToolTip}"/>
                </Grid>
                <StackPanel Grid.Row="1" Orientation="Horizontal" ClipToBounds="True">
                    <TextBlock Text="{Binding Player.ShipTier, Converter={StaticResource ShipTier}}" Width="22" Foreground="#40516A" FontSize="10.5" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Player.ShipName}" Foreground="#576880" FontSize="11" TextTrimming="CharacterEllipsis" ToolTip="{Binding Player.ShipName}"/>
                    <utils:ShipTypeIconBadge ShipType="{Binding Player.ShipType}" TintBrush="{Binding Player.Relation, Converter={StaticResource ShipRelationBrush}}" Width="17" Height="17" Margin="5,0,0,0"/>
                    <TextBlock Text="{Binding KarmaText}" Margin="6,0,0,0" Foreground="{StaticResource DashboardMuted}" FontSize="9" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="ContextTemplate">
            <Grid Margin="6,1">
                <Grid.RowDefinitions><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
                <Grid.ColumnDefinitions><ColumnDefinition Width="22"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <TextBlock Text="{DynamicResource DashboardMetricBattlesShort}" Foreground="{StaticResource DashboardMuted}" FontSize="10.5" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding ContextBattlesText}" Style="{StaticResource CellText}" HorizontalAlignment="Right"/>
                <TextBlock Grid.Row="1" Text="{DynamicResource DashboardMetricWinrateShort}" Foreground="{StaticResource DashboardMuted}" FontSize="10.5" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding ContextWinrateMetric.DisplayValue}" Foreground="{Binding ContextWinrateMetric, Converter={StaticResource MetricBrush}}" FontWeight="SemiBold" FontFamily="Segoe UI" FontSize="12" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding ContextWarning}" Foreground="#B66A00" HorizontalAlignment="Left" ToolTip="{Binding ContextToolTip}"/>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="ShipTemplate">
            <Grid Margin="5,1">
                <Grid.RowDefinitions><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
                <Grid.ColumnDefinitions><ColumnDefinition Width="20"/><ColumnDefinition Width="*"/><ColumnDefinition Width="20"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <TextBlock Text="{DynamicResource DashboardMetricBattlesShort}" Foreground="{StaticResource DashboardMuted}" FontSize="10.5"/>
                <TextBlock Grid.Column="1" Text="{Binding ShipBattlesText}" Style="{StaticResource CellText}" HorizontalAlignment="Right"/>
                <TextBlock Grid.Column="2" Text="{DynamicResource DashboardMetricWinrateShort}" Foreground="{StaticResource DashboardMuted}" FontSize="10.5" Margin="4,0,0,0"/>
                <TextBlock Grid.Column="3" Text="{Binding ShipWinrateMetric.DisplayValue}" Foreground="{Binding ShipWinrateMetric, Converter={StaticResource MetricBrush}}" FontFamily="Segoe UI" FontSize="11.5" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                <TextBlock Grid.Row="1" Text="{DynamicResource DashboardMetricDamageShort}" Foreground="{StaticResource DashboardMuted}" FontSize="10.5"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Text="{Binding ShipDamageText}" Style="{StaticResource CellText}" HorizontalAlignment="Right"/>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="PrTemplate">
            <Grid Margin="6,1">
                <Grid.RowDefinitions><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
                <Grid.ColumnDefinitions><ColumnDefinition Width="22"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <TextBlock Text="{DynamicResource DashboardMetricAccountShort}" Foreground="{StaticResource DashboardMuted}" FontSize="10.5"/>
                <TextBlock Grid.Column="1" Text="{Binding AccountPrMetric.DisplayValue}" Foreground="{Binding AccountPrMetric, Converter={StaticResource MetricBrush}}" FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                <TextBlock Grid.Row="1" Text="{DynamicResource DashboardMetricShipShort}" Foreground="{StaticResource DashboardMuted}" FontSize="10.5"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding ShipPrMetric.DisplayValue}" Foreground="{Binding ShipPrMetric, Converter={StaticResource MetricBrush}}" FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Right"/>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="PerformanceTemplate">
            <Border Background="{Binding Performance.BackgroundColor}" ToolTip="{Binding Performance.ToolTip}" Margin="0">
                <TextBlock Text="{Binding Performance.Icon}" Foreground="{Binding Performance.ForegroundColor}" Opacity="{Binding Performance.IconOpacity}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </DataTemplate>
        <DataTemplate x:Key="ScoreMetricTemplate">
            <Grid Margin="0,1">
                <Grid.ColumnDefinitions><ColumnDefinition Width="112"/><ColumnDefinition Width="94"/><ColumnDefinition Width="112"/></Grid.ColumnDefinitions>
                <TextBlock Text="{Binding AllyText}" Foreground="{StaticResource DashboardBlue}" FontSize="13" FontWeight="SemiBold" FontFamily="Segoe UI" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding Direction}" Foreground="#8090A4" FontSize="10" Margin="0,0,4,0"/>
                    <TextBlock Text="{Binding Label}" Foreground="#63738A" FontSize="10.5" FontWeight="SemiBold"/>
                </StackPanel>
                <TextBlock Grid.Column="2" Text="{Binding EnemyText}" Foreground="{StaticResource DashboardRed}" FontSize="13" FontWeight="SemiBold" FontFamily="Segoe UI"/>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Background="{StaticResource DashboardBackground}">
        <Grid.ColumnDefinitions><ColumnDefinition x:Name="SidebarColumn" Width="172"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
        <Border Grid.Column="0" Background="#F8FBFF" BorderBrush="#DEE7F1" BorderThickness="0,0,1,0">
            <Grid><Grid.RowDefinitions><RowDefinition Height="72"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <Grid Margin="16,12"><Grid.ColumnDefinitions><ColumnDefinition Width="36"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <Border Width="34" Height="34" CornerRadius="17" Background="#E4EFFE"><TextBlock Text="⚓" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#1D4E89"/></Border>
                    <StackPanel x:Name="BrandText" Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Center"><TextBlock Text="ApeRadar EX" FontSize="16" FontWeight="Bold" Foreground="{StaticResource DashboardText}"/><TextBlock Text="{DynamicResource DashboardSubtitle}" FontSize="10.5" Foreground="{StaticResource DashboardMuted}"/></StackPanel>
                </Grid>
                <StackPanel Grid.Row="1" Margin="0,6,0,0">
                    <Button Style="{StaticResource NavButtonSelected}"><StackPanel Orientation="Horizontal"><TextBlock Text="▥" FontSize="19" Width="32"/><TextBlock x:Name="NavBattleText" Text="{DynamicResource DashboardNavBattle}" VerticalAlignment="Center"/></StackPanel></Button>
                    <Button Style="{StaticResource NavButton}" Click="History_Click"><StackPanel Orientation="Horizontal"><TextBlock Text="◴" FontSize="18" Width="32"/><TextBlock x:Name="NavHistoryText" Text="{DynamicResource BtnHistory}" VerticalAlignment="Center"/></StackPanel></Button>
                    <Button Style="{StaticResource NavButton}" Click="ToggleAnalysis_Click"><StackPanel Orientation="Horizontal"><TextBlock Text="⌁" FontSize="20" Width="32"/><TextBlock x:Name="NavAnalysisText" Text="{DynamicResource BtnAnalysis}" VerticalAlignment="Center"/></StackPanel></Button>
                    <Button x:Name="OpenReplayButton" Style="{StaticResource NavButton}" Click="OpenReplay_Click"><StackPanel Orientation="Horizontal"><TextBlock Text="▷" FontSize="20" Width="32"/><TextBlock x:Name="NavReplayText" Text="{DynamicResource BtnOpen}" VerticalAlignment="Center"/></StackPanel></Button>
                    <Button Style="{StaticResource NavButton}" Click="Settings_Click"><StackPanel Orientation="Horizontal"><TextBlock Text="⚙" FontSize="18" Width="32"/><TextBlock x:Name="NavSettingsText" Text="{DynamicResource BtnConfig}" VerticalAlignment="Center"/></StackPanel></Button>
                </StackPanel>
                <StackPanel Grid.Row="2" Margin="10,8,10,16">
                    <Border x:Name="SessionCard" Style="{StaticResource Card}" Padding="8,6" Margin="0,0,0,8" Cursor="Hand" MouseLeftButtonDown="SessionCard_MouseLeftButtonDown" ToolTip="{Binding SessionToolTip, ElementName=Root}">
                        <StackPanel><TextBlock Text="{DynamicResource MainSessionTitle}" FontWeight="SemiBold" FontSize="11"/><TextBlock x:Name="SessionText" Text="—" Foreground="{StaticResource DashboardMuted}" FontSize="10" TextWrapping="Wrap"/></StackPanel>
                    </Border>
                    <TextBlock x:Name="SidebarVersion" Foreground="{StaticResource DashboardMuted}" FontSize="10"><Run Text="{Binding Source={x:Static p:Settings.Default}, Path=SoftwareVersion, Mode=OneWay}"/><Run Text=" · Build "/><Run Text="{Binding Source={x:Static p:Settings.Default}, Path=SoftwareDate, Mode=OneWay}"/></TextBlock>
                    <StackPanel x:Name="SidebarLinks" Orientation="Horizontal" Margin="0,6,0,0"><Button Content="{DynamicResource BtnSoftwareUpdate}" Style="{StaticResource FilterButton}" Click="Update_Click"/><Button Content="{DynamicResource DashboardHelp}" Style="{StaticResource FilterButton}" Click="Help_Click"/></StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions><RowDefinition Height="52"/><RowDefinition Height="*"/></Grid.RowDefinitions>
            <Border Background="White" BorderBrush="#E0E7F0" BorderThickness="0,0,0,1">
                <Grid Margin="18,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{DynamicResource DashboardDataSource}" Foreground="{StaticResource DashboardMuted}" FontSize="12"/>
                        <TextBlock Text="{Binding Metadata.Provider}" Foreground="{StaticResource DashboardText}" FontWeight="SemiBold" FontSize="12"/>
                        <TextBlock Text="   " FontSize="12"/><TextBlock Text="{DynamicResource DashboardLastUpdated}" Foreground="{StaticResource DashboardMuted}" FontSize="12"/>
                        <TextBlock Text="{Binding Metadata.UpdatedAtText}" Foreground="{StaticResource DashboardText}" FontSize="12"/>
                        <Button x:Name="RefreshButton" Style="{StaticResource ToolbarButton}" Content="↻" Width="34" Padding="0" Margin="10,0,0,0" Click="Refresh_Click"/>
                        <TextBlock Text="   " FontSize="12"/><TextBlock Text="{Binding Source={x:Static p:Settings.Default}, Path=GamePath, Converter={StaticResource CvtGamePathIsSetText}}" Foreground="{Binding Source={x:Static p:Settings.Default}, Path=GamePath, Converter={StaticResource CvtGamePathIsSetColor}}" FontSize="12" FontWeight="SemiBold"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="StatusButton" Visibility="{Binding Status.IsVisible, ElementName=Root, Converter={StaticResource BoolToVisibility}}" Background="Transparent" BorderThickness="0" Click="Status_Click" ToolTip="{Binding Status.Text, ElementName=Root}"><StackPanel Orientation="Horizontal"><Ellipse Width="10" Height="10" Fill="{Binding Status.Accent, ElementName=Root}" Margin="0,0,7,0"/><TextBlock Text="{Binding Status.Text, ElementName=Root}" Foreground="#40516A" FontWeight="SemiBold" FontSize="11.5" MaxWidth="220" TextTrimming="CharacterEllipsis"/></StackPanel></Button>
                        <ComboBox x:Name="LanguageCombo" Width="88" Margin="12,0,0,0" SelectionChanged="LanguageCombo_SelectionChanged"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="18,12,18,12">
                <Grid.RowDefinitions><RowDefinition Height="42"/><RowDefinition Height="106"/><RowDefinition Height="*" MinHeight="250"/><RowDefinition Height="52"/></Grid.RowDefinitions>
                <Grid Margin="0,0,0,8"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBlock Text="{DynamicResource DashboardNavBattle}" FontSize="23" FontWeight="Bold" Foreground="{StaticResource DashboardText}" VerticalAlignment="Center"/>
                    <Border Grid.Column="1" Visibility="{Binding Status.IsVisible, ElementName=Root, Converter={StaticResource BoolToVisibility}}" Background="#E1F7EA" CornerRadius="8" Padding="10,4" Margin="13,0,0,0"><TextBlock Text="{Binding Status.Text, ElementName=Root}" Foreground="#168653" FontSize="11.5" FontWeight="SemiBold"/></Border>
                    <TextBlock Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{StaticResource DashboardMuted}" FontSize="11.5">
                        <Run Text="{DynamicResource DashboardMap}"/><Run Text="{Binding Metadata.MapName, Mode=OneWay}"/><Run Text="   |   "/><Run Text="{DynamicResource DashboardMode}"/><Run Text="{Binding Metadata.Mode, Mode=OneWay}"/><Run Text="   |   "/><Run Text="{DynamicResource DashboardServer}"/><Run Text="{Binding Metadata.Server, Mode=OneWay}"/><Run Text="   |   "/><Run Text="{DynamicResource DashboardStartedAt}"/><Run Text="{Binding Metadata.StartedAtText, Mode=OneWay}"/>
                    </TextBlock>
                    <StackPanel Grid.Column="3" Orientation="Horizontal" Margin="10,0,0,0">
                        <ComboBox x:Name="SortCombo" Width="176" Height="32" Margin="4,0" SelectionChanged="SortCombo_SelectionChanged"/>
                        <Button Style="{StaticResource ToolbarButton}" Content="{DynamicResource DashboardSaveScreenshot}" Click="Screenshot_Click"/>
                    </StackPanel>
                </Grid>

                <Border Grid.Row="1" Style="{StaticResource Card}" Height="98" Padding="12,7">
                    <Border.Background><LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5"><GradientStop Color="#F1F7FF" Offset="0"/><GradientStop Color="#FFFFFF" Offset="0.42"/><GradientStop Color="#FFFFFF" Offset="0.58"/><GradientStop Color="#FFF3F5" Offset="1"/></LinearGradientBrush></Border.Background>
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition x:Name="AllyMatchupColumn" Width="*"/><ColumnDefinition x:Name="AllyMatchupSeparatorColumn" Width="1"/><ColumnDefinition x:Name="ComparisonColumn" Width="430"/><ColumnDefinition x:Name="EnemyMatchupSeparatorColumn" Width="1"/><ColumnDefinition x:Name="EnemyMatchupColumn" Width="*"/></Grid.ColumnDefinitions>
                        <Grid x:Name="AllyMatchupPanel" Margin="8,0,18,0"><Grid.RowDefinitions><RowDefinition Height="24"/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
                            <TextBlock Text="{DynamicResource DashboardAllyKeyShips}" Foreground="{StaticResource DashboardBlue}" FontSize="11.5" FontWeight="Bold" HorizontalAlignment="Right"/>
                            <TextBlock Grid.Row="1" Text="{Binding Summary.Carrier.AllySummaryText}" Visibility="{Binding Summary.Carrier.IsVisible, Converter={StaticResource BoolToVisibility}}" Foreground="#40516A" FontSize="11" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="2" Text="{Binding Summary.Destroyer.AllySummaryText}" Visibility="{Binding Summary.Destroyer.IsVisible, Converter={StaticResource BoolToVisibility}}" Foreground="#40516A" FontSize="11" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>
                        <Border Grid.Column="1" Background="#D9E3EF" Margin="0,3"/>
                        <Grid Grid.Column="2"><Grid.RowDefinitions><RowDefinition Height="19"/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="110"/><ColumnDefinition Width="88"/><ColumnDefinition Width="110"/></Grid.ColumnDefinitions><TextBlock Text="{Binding Summary.AllyCoverageText}" Foreground="{StaticResource DashboardBlue}" FontSize="10.5" FontWeight="SemiBold" HorizontalAlignment="Right"/><TextBlock Grid.Column="1" Text="{DynamicResource DashboardVersusMetrics}" Foreground="#40516A" FontSize="11.5" FontWeight="Bold" HorizontalAlignment="Center" ToolTip="{Binding Summary.CoverageNotice}"/><TextBlock Grid.Column="2" Text="{Binding Summary.EnemyCoverageText}" Foreground="{StaticResource DashboardRed}" FontSize="10.5" FontWeight="SemiBold"/></Grid>
                            <ContentControl Grid.Row="1" Content="{Binding Summary.ContextWinrate}" ContentTemplate="{StaticResource ScoreMetricTemplate}"/>
                            <ContentControl Grid.Row="2" Content="{Binding Summary.ContextPr}" ContentTemplate="{StaticResource ScoreMetricTemplate}"/>
                            <ContentControl Grid.Row="3" Content="{Binding Summary.ShipWinrate}" ContentTemplate="{StaticResource ScoreMetricTemplate}"/>
                            <ContentControl Grid.Row="4" Content="{Binding Summary.ShipPr}" ContentTemplate="{StaticResource ScoreMetricTemplate}"/>
                        </Grid>
                        <Border Grid.Column="3" Background="#D9E3EF" Margin="0,3"/>
                        <Grid x:Name="EnemyMatchupPanel" Grid.Column="4" Margin="18,0,8,0"><Grid.RowDefinitions><RowDefinition Height="24"/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
                            <TextBlock Text="{DynamicResource DashboardEnemyKeyShips}" Foreground="{StaticResource DashboardRed}" FontSize="11.5" FontWeight="Bold"/>
                            <TextBlock Grid.Row="1" Text="{Binding Summary.Carrier.EnemySummaryText}" Visibility="{Binding Summary.Carrier.IsVisible, Converter={StaticResource BoolToVisibility}}" Foreground="#40516A" FontSize="11" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="2" Text="{Binding Summary.Destroyer.EnemySummaryText}" Visibility="{Binding Summary.Destroyer.IsVisible, Converter={StaticResource BoolToVisibility}}" Foreground="#40516A" FontSize="11" VerticalAlignment="Center"/>
                        </Grid>
                    </Grid>
                </Border>

                <Grid Grid.Row="2" Margin="0,8,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="8"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Style="{StaticResource Card}" ClipToBounds="True"><Grid><Grid.RowDefinitions><RowDefinition Height="40"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                        <Border Background="{StaticResource DashboardAllySurface}" BorderBrush="#C9DDFC" BorderThickness="0,0,0,1"><Grid Margin="10,0"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Foreground="{StaticResource DashboardBlue}" FontSize="14.5" FontWeight="Bold" VerticalAlignment="Center"><Run Text="{DynamicResource LabelAllies}"/><Run Text=" ("/><Run Text="{Binding AllyTotalCount, Mode=OneWay}"/><Run Text=")"/></TextBlock><StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center"><Button x:Name="AllyAll" Tag="Ally:All" Style="{StaticResource FilterButtonActive}" Click="Filter_Click"/><Button x:Name="AllyMarked" Tag="Ally:Marked" Style="{StaticResource FilterButton}" Click="Filter_Click"/><Button x:Name="AllyLow" Tag="Ally:LowSample" Style="{StaticResource FilterButton}" Click="Filter_Click"/><Button x:Name="AllyAnomaly" Tag="Ally:Anomaly" Style="{StaticResource FilterButton}" Click="Filter_Click"/></StackPanel></Grid></Border>
                        <DataGrid x:Name="AlliesGrid" Grid.Row="1" Style="{StaticResource RosterGrid}" RowStyle="{StaticResource RosterRowStyle}" ItemsSource="{Binding Allies}" LoadingRow="RosterGrid_LoadingRow">
                            <DataGrid.Columns><DataGridTextColumn Header="#" Binding="{Binding OriginalOrder}" Width="28"/><DataGridTemplateColumn Header="{DynamicResource DashboardColumnPlayer}" CellTemplate="{StaticResource PlayerTemplate}" Width="*" MinWidth="150"/><DataGridTemplateColumn x:Name="AllyContextColumn" Header="{DynamicResource DashboardContextAccount}" CellTemplate="{StaticResource ContextTemplate}" Width="82"/><DataGridTemplateColumn x:Name="AllyShipColumn" Header="{DynamicResource DashboardColumnCurrentShip}" CellTemplate="{StaticResource ShipTemplate}" Width="122"/><DataGridTemplateColumn x:Name="AllyPrColumn" Header="PR" CellTemplate="{StaticResource PrTemplate}" Width="72"/><DataGridTemplateColumn x:Name="AllyPerformanceColumn" Header="{DynamicResource DashboardColumnPerformance}" CellTemplate="{StaticResource PerformanceTemplate}" Width="45"/></DataGrid.Columns>
                        </DataGrid>
                    </Grid></Border>
                    <Border Grid.Column="2" Style="{StaticResource Card}" ClipToBounds="True"><Grid><Grid.RowDefinitions><RowDefinition Height="40"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                        <Border Background="{StaticResource DashboardEnemySurface}" BorderBrush="#F8CCD3" BorderThickness="0,0,0,1"><Grid Margin="10,0"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><TextBlock Foreground="{StaticResource DashboardRed}" FontSize="14.5" FontWeight="Bold" VerticalAlignment="Center"><Run Text="{DynamicResource LabelEnemies}"/><Run Text=" ("/><Run Text="{Binding EnemyTotalCount, Mode=OneWay}"/><Run Text=")"/></TextBlock><StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center"><Button x:Name="EnemyAll" Tag="Enemy:All" Style="{StaticResource FilterButtonActive}" Click="Filter_Click"/><Button x:Name="EnemyMarked" Tag="Enemy:Marked" Style="{StaticResource FilterButton}" Click="Filter_Click"/><Button x:Name="EnemyLow" Tag="Enemy:LowSample" Style="{StaticResource FilterButton}" Click="Filter_Click"/><Button x:Name="EnemyAnomaly" Tag="Enemy:Anomaly" Style="{StaticResource FilterButton}" Click="Filter_Click"/></StackPanel></Grid></Border>
                        <DataGrid x:Name="EnemiesGrid" Grid.Row="1" Style="{StaticResource RosterGrid}" RowStyle="{StaticResource RosterRowStyle}" ItemsSource="{Binding Enemies}" LoadingRow="RosterGrid_LoadingRow">
                            <DataGrid.Columns><DataGridTextColumn Header="#" Binding="{Binding OriginalOrder}" Width="28"/><DataGridTemplateColumn Header="{DynamicResource DashboardColumnPlayer}" CellTemplate="{StaticResource PlayerTemplate}" Width="*" MinWidth="150"/><DataGridTemplateColumn x:Name="EnemyContextColumn" Header="{DynamicResource DashboardContextAccount}" CellTemplate="{StaticResource ContextTemplate}" Width="82"/><DataGridTemplateColumn x:Name="EnemyShipColumn" Header="{DynamicResource DashboardColumnCurrentShip}" CellTemplate="{StaticResource ShipTemplate}" Width="122"/><DataGridTemplateColumn x:Name="EnemyPrColumn" Header="PR" CellTemplate="{StaticResource PrTemplate}" Width="72"/><DataGridTemplateColumn x:Name="EnemyPerformanceColumn" Header="{DynamicResource DashboardColumnPerformance}" CellTemplate="{StaticResource PerformanceTemplate}" Width="45"/></DataGrid.Columns>
                        </DataGrid>
                    </Grid></Border>
                </Grid>

                <Border Grid.Row="3" Style="{StaticResource Card}" Margin="0,8,0,0" Padding="10,5"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="105"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBlock Text="{DynamicResource DashboardObservations}" FontWeight="Bold" FontSize="13" Foreground="{StaticResource DashboardText}" VerticalAlignment="Center"/>
                    <WrapPanel Grid.Column="1" VerticalAlignment="Center"><Border Background="#EDF5FF" CornerRadius="7" Margin="4,0" Padding="10,5" ToolTip="{Binding Summary.CoverageNotice}"><TextBlock Text="{Binding Summary.CoverageObservation}" Foreground="#176ADE" FontSize="10.5" FontWeight="SemiBold"/></Border><Border Background="#F4EEFF" CornerRadius="7" Margin="4,0" Padding="10,5"><TextBlock Text="{Binding Summary.LowSampleObservation}" Foreground="#7541C9" FontSize="10.5" FontWeight="SemiBold"/></Border><Border Background="#FFF7E8" CornerRadius="7" Margin="4,0" Padding="10,5"><TextBlock Text="{Binding Summary.AnomalyObservation}" Foreground="#B26B00" FontSize="10.5" FontWeight="SemiBold"/></Border></WrapPanel>
                    <Border Grid.Column="2" Background="#EFF3F8" BorderBrush="#D5DFEB" BorderThickness="1" CornerRadius="7" Padding="3"><StackPanel Orientation="Horizontal"><Button x:Name="AccountContext" Content="{DynamicResource DashboardContextAccount}" Tag="Account" Style="{StaticResource SegmentActive}" Click="Context_Click"/><Button x:Name="TierContext" Content="{DynamicResource DashboardContextTier}" Tag="Tier" Style="{StaticResource SegmentButton}" Click="Context_Click"/></StackPanel></Border>
                </Grid></Border>
            </Grid>

            <Border x:Name="AnalysisDrawer" Grid.RowSpan="2" Width="420" HorizontalAlignment="Right" Background="White" BorderBrush="#CFD9E6" BorderThickness="1,0,0,0" Panel.ZIndex="30" Visibility="Collapsed" Padding="10">
                <Border.Effect><DropShadowEffect Color="#29415F" BlurRadius="18" ShadowDepth="-3" Opacity="0.22"/></Border.Effect>
                <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="3*"/><RowDefinition Height="1*"/><RowDefinition Height="92"/></Grid.RowDefinitions>
                    <DockPanel Margin="4,0,0,6"><Button DockPanel.Dock="Right" Content="×" Width="30" Height="30" Click="ToggleAnalysis_Click"/><TextBlock Text="{DynamicResource AnalysisPanelTitle}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/></DockPanel>
                    <lvc:CartesianChart Grid.Row="1" x:Name="DashboardWinrateChart" TooltipTextSize="13" TooltipFindingStrategy="CompareAllTakeClosest" Margin="0,-8,0,-8"/>
                    <lvc:CartesianChart Grid.Row="2" x:Name="DashboardKdeChart" Margin="0,2,0,0"/>
                    <TextBox Grid.Row="3" x:Name="DashboardOutputText" Margin="0,6,0,0" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" IsReadOnly="True"/>
                </Grid>
            </Border>
        </Grid>

        <Popup x:Name="NotificationPopup" PlacementTarget="{Binding ElementName=StatusButton}" Placement="Bottom" StaysOpen="False" AllowsTransparency="True">
            <Border Width="560" Height="170" Style="{StaticResource Card}" Padding="6" Margin="0,5,0,0"><DataGrid HeadersVisibility="Column" RowHeight="27" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" ItemsSource="{Binding Source={x:Static utils:NotificationMessageUtils.NotificationMessageCollection}}"><DataGrid.Columns><DataGridTextColumn Header="{DynamicResource DataGridHeaderTime}" Binding="{Binding Time, StringFormat='{}{0:HH:mm:ss}'}" Width="78"/><DataGridTextColumn Header="{DynamicResource DataGridHeaderMessage}" Binding="{Binding Message}" Width="*"/></DataGrid.Columns></DataGrid></Border>
        </Popup>
        <Popup x:Name="PlayerDetailPopup" Placement="Custom" StaysOpen="True" AllowsTransparency="True" PopupAnimation="Fade" MouseEnter="PlayerDetailPopup_MouseEnter" MouseLeave="PlayerDetailPopup_MouseLeave">
            <Border x:Name="PlayerDetailBorder" Width="560" MaxHeight="720" Style="{StaticResource Card}" Padding="10" Margin="6"><ScrollViewer VerticalScrollBarVisibility="Auto"><controls:PlayerDetailCard x:Name="PlayerDetailCardContent"/></ScrollViewer></Border>
        </Popup>
    </Grid>
</UserControl>
